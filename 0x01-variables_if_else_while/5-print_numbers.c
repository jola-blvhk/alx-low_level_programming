#include <stdio.h>
/**
 * main - Entry point
 *
 * Return: always 0 (success)
 */
int main(void)

{
int digit;

for (digit = 0; digit < 10; digit++)
{
	printf("%d", digit);
}

printf("\n");

return (0);
}
